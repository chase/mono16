box: chase/fontforge@0.2.0
build:
    steps:
        - script:
            name: build the release
            code: |
                python build.py
deploy:
    steps:
        - chase/ghr:
            token: $GITHUB_TOKEN
            input: release
            replace: true
